import 'package:flutter/material.dart';

class PlayGamePage extends StatelessWidget {
  const PlayGamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Play Game Page')));
  }
}
